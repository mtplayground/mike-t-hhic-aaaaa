# mike-t-hhic-aaaaa

Vite + React + TypeScript scaffold for the calculator project.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Local development

The Vite dev server is configured to bind to `0.0.0.0:8080`.
